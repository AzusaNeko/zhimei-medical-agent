# 智美医美顾问 v2.0 · 功能需求说明书

> 本文写的是**需求**：要做什么、做到什么程度算合格、怎么验证。
> 工程实现与踩坑记录见 [`Code/README.md`](Code/README.md)，
> 真实依赖下的逐项自测清单见 [`Code/SELF-TEST.md`](Code/SELF-TEST.md)，
> 工作流设计图见 [`Workflow/`](Workflow/)。

## 0. 一句话定位

一个**面向医美机构的 AI 顾问系统**：C 端顾客可以咨询项目、查门店资质、改约，
坐席端可以接管 AI 答不了或不该答的问题。核心约束只有一条 ——
**AI 说的话必须经过审查才能出站，审查不过就转人工，绝不"装作通过"。**

## 1. 角色与边界

| 角色 | 入口 | 能做什么 |
|---|---|---|
| 顾客 | `/chat` | 咨询、查资质、改约、看自己的历史对话、要求转人工、删除自己的对话 |
| 客服（service） | `/ops/panel` | 接单接管、回复、关单、标记误报；**看不到**明文联系方式 |
| 医师（doctor） | `/ops/panel` | 同上，且可见明文 PII、可被升级接诊 |
| 合规（compliance） | `/ops/panel` | 可见明文 PII 与全部风险报告；**无权回复**用户 |
| 管理员（admin） | `/ops/panel` | 全部权限，外加清理测试工单 |

> 身份规则：**令牌说你是谁，数据库说你能干什么。**
> 角色一律从数据库按 `agent_id` 现读，不接受客户端声明、也不信令牌里的旧角色。

---

## 2. 顾客端（C 端）功能需求

### 2.1 账号与会话

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F1.1.1 | 注册 | 邮箱 + 口令；口令用 `scrypt` 加盐存储（参数写在哈希串里，便于日后调强度） |
| F1.1.2 | 邮箱验证（占位） | 注册后必须验证邮箱才能登录；无邮件服务时验证令牌在响应里返回（`EXPOSE_VERIFY_TOKEN`），接真实邮件后必须关闭 |
| F1.1.3 | 登录 | 返回 JWT（HS256、显式算法白名单、强制校验 `exp`/`iss`） |
| F1.1.4 | **防账号枚举** | 口令错与账号不存在返回**同一个错误码**；账号不存在时也跑一次哈希（防时序枚举） |
| F1.1.5 | 刷新保持登录 | 刷新页面不掉登录；**只有 401/403 才清令牌**，500 或网络抖动一律保留并如实报错 |

### 2.2 对话

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F1.2.1 | 多轮对话 | 一次提问走完整图；过程状态逐条出现，**正文整段审后到达** |
| F1.2.2 | 正文呈现 | 逐字呈现（打字机）；**点一下立刻显示全文**；尊重 `prefers-reduced-motion`；角标写明"整段过审后送达" |
| F1.2.3 | **会话内记忆** | 能接住指代（"那个更适合我"）；用户说过的事实要沿用（含**没有对应槽位**的事实，如过敏史） |
| F1.2.4 | 紧急情形 | 命中急诊词表 → **固定模板**提示 + 建 P0 工单，措辞**不走模型生成** |
| F1.2.5 | 操作类请求 | 改约等操作先挂起（`awaiting_confirmation`）→ 用户确认 → **幂等执行** → 执行结果再送审一次 |
| F1.2.6 | 硬性阻断 | 命中硬规则 → 不出任何业务正文，如实告知被阻断与命中的规则编号 |

### 2.3 历史对话

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F1.3.1 | 列表 | 标题自动取自**第一条用户消息**（不用手动命名）；带消息数、相对时间、人工接管标记 |
| F1.3.2 | 新建 | 「＋ 新对话」清空对话区与轨迹，**但不立刻建会话**（发第一条消息时才建，免得堆空会话） |
| F1.3.3 | 切换 | 点历史会话完整恢复消息记录，并回到该会话的接管状态 |
| F1.3.4 | **删除** | 每条右侧有删除按钮；确认框**逐条说明删什么、留什么**；删除后回到新对话态 |
| F1.3.5 | 删除的范围 | **真删**：消息 + 会话行 + 图状态（`lg.*`）+ 工单里的对话快照与草稿。**保留**：内容审查的裁决记录（合规要求），并在工单上标注"对话原文已被顾客删除" |
| F1.3.6 | 删除的保护 | 会话还有**未结束工单**时拒绝删除（409 并说明原因）——人工正在处理，把会话抽走会让坐席对着空详情页 |

### 2.4 执行过程可视化

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F1.4.1 | 节点执行轨迹 | 按顺序实时列出真正执行过的节点（含子图内部节点），带中文名、耗时、是否调用大模型 |
| F1.4.2 | 展开节点输出 | 点任意节点可展开它写回的状态（意图/槽位、检索候选、证据筛选、逐句核对、裁决表） |
| F1.4.3 | **工作流架构图** | 按 `Workflow/langgraph-main.mmd` 分六层绘制；走过的节点依次点亮、走过的连线流动；菱形=路由点 |
| F1.4.4 | **本次经过** | 明确显示这一次经过了**哪几层、哪几个子图、各几步**、草稿层跑了哪几个 Agent |
| F1.4.5 | 同屏但分窗 | 架构与轨迹**同时可见**，各自独立滚动（滚一个不会把另一个滚跑） |
| F1.4.6 | 架构不漂移 | 架构与代码的一致性由后端交叉校验；不一致必须显示出来（而不是悄悄画错） |
| F1.4.7 | 调试视图警告 | 轨迹可能含**未经审查的中间产物**，界面必须写明"面向真实顾客时要关掉" |

### 2.5 转人工（顾客视角）

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F1.5.1 | 系统判定转人工 | 急诊 / 高风险 / 修订预算耗尽 / 澄清预算耗尽 → 建工单并告知用户 |
| F1.5.2 | **用户主动要求转人工** | 第 1–2 次给**安抚回复**（如实说明"连续 3 次即转接"并报出当前次数）；第 3 次真的转接（建单 + **当场停用 AI**） |
| F1.5.3 | 识别的准确度 | "人工客服几点下班""人工费怎么算"这类**打听信息**不算转人工请求 |
| F1.5.4 | 紧急优先 | "我脸发白还特别疼，麻烦转人工"必须走急诊路径（急诊模板 + 转人工并行），不能被"转人工"这条拦走 |
| F1.5.5 | **接管期间仍可发言** | 输入框**不禁用**；消息照常落库并转给坐席；AI **不出正文**（不抢话） |
| F1.5.6 | 接管回执不刷屏 | 接管开始后**第一条**给完整说明，之后只走状态栏提示 |
| F1.5.7 | 关单后 AI 恢复 | 坐席关单 → AI 自动恢复作答；顾客再说话是找 AI 的，**不再推给人工** |
| F1.5.8 | 接管期间的确认操作 | 用户点"确认执行"**不得执行**（不可逆操作 + 人工已在场），回执里显式写明"没有执行" |

---

## 3. 坐席端（运营后台）功能需求

### 3.1 登录与权限

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F2.1.1 | 坐席登录 | 与顾客同一套 JWT 体系；角色来自数据库 |
| F2.1.2 | 权限下发 | 把角色的 `{"*"}` **展开成具体权限名**再给前端（前端只做显示，判定仍在服务端） |
| F2.1.3 | 脱敏按角色 | `service` 看到 `138****8000`；`doctor`/`compliance`/`admin` 见明文 |

### 3.2 待处理队列

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F2.2.1 | **只列待处理** | 默认只返回 `open/accepted/in_progress/escalated`；已结束的离开队列；查历史需显式 `?status=closed` |
| F2.2.2 | 排序 | 按优先级（P0→P2）再按创建时间 |
| F2.2.3 | **等待时长** | 每行显示已等待时长与 SLA 基准；超时变红加粗 |
| F2.2.4 | **脱敏发起人** | 只显示名字**第一个字**，其余打码（如 `演***`）；看全名点进详情（按角色权限） |
| F2.2.5 | 一行上下文 | 转人工原因 + 画像摘要首行（否则坐席得点进去才知道要不要先接） |
| F2.2.6 | 实时推送 | SSE 每 3 秒一帧快照；队列变化无需手动刷新 |
| F2.2.7 | 顾客补充可见 | 接管期间顾客又说话 → 详情**自动刷新**并提示（仅对未结束工单） |
| F2.2.8 | 测试工单隔离 | 自动化测试造的工单默认不进队列；勾选后可见且带「测试」徽章 |
| F2.2.9 | 清理测试工单 | 需 `admin:purge` 权限；**只删 `is_test=true`** 是代码里的硬约束，真实工单绝不受影响 |
| F2.2.10 | **按风险等级分窗** | 队列分**高 / 中 / 低**三块展示，每行标注等级与**判定理由**（"为什么是高/中/低风险"），座席先看哪一窗一目了然 |
| F2.2.11 | **风险等级 ≠ 调度优先级** | 风险等级来自内容（体检/审查结论），`P0/P1` 来自调度（急诊/预算耗尽/用户要求）。**两者分开标注、不互相替代** —— 把 P0 当成"高风险"会让"这条到底危险不危险"失去依据 |
| F2.2.12 | **结束后立即移出** | 工单一旦结束（`closed` 等）立刻离开待处理队列；详情页对已结束工单显示"已结束"横幅，并**停止跟踪**其后续消息（人工已退场，不该再收到该顾客的推送） |

### 3.3 工单处置

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F2.3.1 | 接单接管 | 写 `accepted_at` 并**同时停用 AI**（"AI 不与坐席抢话"的唯一开关） |
| F2.3.2 | 未接单不得承诺 | `accepted_at` 为空时 `human_joined=false`，且未接单直接回复被 409 拒绝 |
| F2.3.3 | 坐席回复 | 走**规则层轻校验，不调 LLM**：`block` 硬拦（隐私泄露）、`revise` 软提示（疗效承诺/绝对化用语，确认后可发） |
| F2.3.4 | 代执行拦截 | "已为您取消/已帮您退"这类替用户办事的表述一律拦 |
| F2.3.5 | 转医师 / 关单 / 重开 | 关单恢复 AI；重开重新停用 AI |
| F2.3.6 | 标记误报 | 紧急词的调优数据来源，落 `ops.misreport_feedback` |
| F2.3.7 | 详情完整性 | 画像摘要 / 最近对话 / 风险报告（含 **AI 未发出的草稿与审查意见**）/ 发起人 / 处置事件 |

### 3.4 指标看板

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F2.4.1 | 工单指标 | 总数 / 未结束 / P0 未结束 / 已接单 / 平均等待时长 |
| F2.4.2 | **排除测试工单** | SLA 指标绝不能被测试数据污染（测试数据进指标比进队列更危险：队列里看得见，指标里会被当成结论用） |
| F2.4.3 | 审查指标 | 审查轮次、一次通过率、同族复核计数、硬规则 Top |

---

## 4. Agent 编排与风控（系统能力）

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F3.1 | 九 Agent 拓扑 | 入口调度 → 七路草稿产出（科普/推荐/资质/预约/术后/澄清/急诊）→ 聚合 → **必经**风险审查 → 输出与执行 → 出口层 |
| F3.2 | **强制风险门** | 所有出站都必须过：硬性规则 + 三份面板意见（医疗/广告/隐私）→ 裁决 → 签发绑定 `content_hash` + `plan_hash` 的放行凭据 |
| F3.3 | 出口校验 | `send` / `execute_op` 开头校验凭据；改一个字节即作废 |
| F3.4 | 五档出口 | `pass` / `revise` / `need_info` / `block` / `human`，各有明确去向 |
| F3.5 | 三套预算 | `revision_count`（每轮，上限 2）、`clarify_count`（跨轮，上限 2）、`kb_verify_round`（子图内，上限 3） |
| F3.6 | 预算耗尽不一律降级 | 软问题（措辞过头）修一轮就放行；硬问题（无依据）预算耗尽才降级 —— 避免"越改越空、最后丢弃" |
| F3.7 | 子图自环防死循环 | `kb_limit → kb_return` 而非回 `kb_verify`（否则死循环、每圈调一次 LLM） |
| F3.8 | **判定类节点不喂历史** | 逐句核对只喂"草稿 + 证据"：否则模型会拿"用户说过/助手说过"当依据，把"引用可溯源"整条保证悄悄换掉 |
| F3.9 | 跨轮记忆 | 最近约 5 轮对话注入**回答类**节点；槽位跨轮沿用；越界问题先用已有上下文试答一次 |
| F3.10 | 兜底转人工 | 图执行异常 / 步数超限 → 建工单 + 发 error 事件 + **写服务端日志（含堆栈）** |
| F3.11 | 不假装成功 | 审查失败不得当成通过；被阻断的操作回执里显式标注"未执行" |

---

## 4b. 知识库（RAG 地基）

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F3.12 | 规模与粒度 | 15 篇 / 84 个 chunk。**chunk 是检索与引用的单位** —— 整篇一块的话，问哪一半都会把整篇塞进证据，"逐句核对引用"随之失去意义 |
| F3.13 | 覆盖范围 | 8 个项目文档（热玛吉/超声炮/水光针/光子嫩肤/皮秒/线雕/瘦脸针/玻尿酸）+ 7 篇通用（术前面诊、风险与紧急信号、术后护理、资质核实、项目对比、费用口径、常见问题） |
| F3.14 | **通用资料必须对所有项目可见** | 通用资料的 `project` 为空串，项目过滤必须写成 `project in [...] or project == ""`。只写前者会把"需要立即就医的信号"这类资料**整体排除**，且不留任何痕迹 |
| F3.15 | 口语问法可召回 | 安全关键条目带"顾客常问：…"的口语问句引导，且**不改动医学结论原文**（便于逐条复核）。精排对口语↔书面语的用词差异很敏感，实测同一问题从 0.0091 提升到 0.927 |
| F3.16 | 幂等与对账 | 重复播种不产生重复向量；文档改名/删除后，旧文档与旧向量必须一并消失。对账基准是 **Milvus 实际有什么**，不是"我以为我写过什么" |
| F3.17 | 检索验收 | `check_retrieval.py` 用带**期望 doc_id** 的 15 条用例把关；安全关键用例还须真的留下证据，否则脚本失败（"检索到了却因分数不够而不用"是危险降级） |
| F3.18 | **证据准入 = 绝对下限 + 相对系数** | 不能只用一条绝对分数线：精排分是 sigmoid 概率，**绝对值取决于问法与文风**。实测不相关问题最高 0.0002、最低的相关证据 0.0548，而"皮秒会不会反黑"这种正确命中只有 0.183 —— 一条 0.30 的线卡掉的**全是相关的**。故：`RERANK_FLOOR`（判断是否全都不相关）+ `RERANK_REL_RATIO`（只留最高分附近那一簇） |
| F3.19 | **弱证据必须加复核** | `MIN_RERANK` 不再作准入门槛，而是"弱证据"信号：没到它就**额外走一次"证据是否充分"的独立判断**。放松闸门必须配上更严的复核，不能只放松 |
| F3.20 | 拒绝凭空作答的保护不可移除 | "全都不相关 → 一条证据都不要"必须保留；放宽的是"相关但分数低"，不是"什么都不相关也照答" |

> ⚠️ 知识库内容是**演示材料**，未经医学/法务审定。自测能验"能否检索到、引用是否指向具体块、
> 项目过滤是否误伤通用资料"，**验不了"内容对不对"**。

---

## 5. 数据与隐私

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F4.1 | 服务端脱敏 | 手机号 / 身份证 / 银行卡 / 邮箱一律在**服务端**出口脱敏，前端拿不到原文 |
| F4.2 | 会话隔离 | 会话归属由令牌决定，不接受请求体传入 `user_id`；跨用户访问返回 **404 而非 403** |
| F4.3 | 列表按用户过滤 | `/api/sessions` 只返回当前登录用户的会话 |
| F4.4 | 队列里的最小暴露 | 一屏几十行的列表只给首字；全名只在详情里、按角色给 |
| F4.5 | **删除权** | 顾客可删除自己的对话；删除是**真删**（含另一个 schema 里的图状态）；保留审计的部分必须如实告知 |
| F4.6 | 审计不可删 | 内容审查的裁决与规则命中保留 —— "顾客说过什么"可删，"系统为什么放行/拦截"不可删 |

---

## 6. 运维与工程保障

| 编号 | 需求 | 验收要点 |
|---|---|---|
| F5.1 | 双档位 | `fake` 档位零外部依赖（内存存储 + 脚本化假模型）用于回归；`real` 档位接 Postgres + Milvus + 真实模型 |
| F5.2 | 存储层一致性 | `PgStore` 与 `FakePg` 的方法签名逐字一致（`check_store_parity`），否则会出现"fake 下全绿、上真库就崩" |
| F5.3 | **schema 升级路径** | 重跑 `schema.sql` 必须真的能升级已有库（含幂等 `ADD COLUMN`）；新建库与升级旧库**两条路径都自动验证**（`check_schema`） |
| F5.4 | 环境体检 | `check_env` 逐项检查依赖/端口/表/知识库，并打印"下一步该做什么" |
| F5.5 | 前端一致性 | `check_ui` 检查 JS 语法、节点表与后端 `add_node` 双向一致、DOM id、测试工单隔离、页面**真的能启动**、接管状态、正文呈现、删除流程、弹窗与可拉伸布局 |
| F5.6 | 跨轮行为 | `multi_turn_cases.py` 覆盖只有跨轮才暴露的行为（指代、非槽位事实、槽位覆盖、澄清预算、接管全时序、会话隔离） |
| F5.7 | 页面不缓存 | `/chat` 与 `/ops/panel` 带 `Cache-Control: no-store` —— 单文件页面边改边用，缓存住旧版本会把排查方向带偏 |
| F5.8 | 端口不与系统冲突 | docker 端口避开 Windows 保留区间（MinIO 9001→9100 等），并在 compose 里写明理由 |

---

## 7. 明确不做（MVP 范围边界）

写清楚"不做什么"和写清楚"做什么"一样重要 —— 否则这些会被当成缺陷反复报。

| 不做 | 原因 |
|---|---|
| **真 · token 流式正文** | 正文必须整段过风险闸才能出站；按 token 吐 = 用户先看到**未经审查**的内容。且草稿本身是 JSON，流出来是 JSON 碎片。现在做的是"送达之后的逐字呈现" |
| 跨会话长期记忆 / 向量召回 | 当前是**会话内**记忆（最近约 5 轮 + 槽位沿用）。超出窗口的信息只能靠槽位，再久就丢 |
| 多模态看图 | 图片只入库不解析；上传图片一律转人工，且**不假装看过** |
| 真实邮件服务 | 邮箱验证是占位：无邮件服务时验证令牌直接在响应里返回，接真实邮件后必须关掉 `EXPOSE_VERIFY_TOKEN` |
| Redis pub/sub / LISTEN-NOTIFY | 队列实时性用服务端 3 秒轮询推送；前端契约（`snapshot`/`alert`）已定，换后端实现不动前端 |
| 真实支付 / 真实机构业务系统 | 门店/医生/项目/档期/预约在 MVP 里是本地模拟表，上线前替换为机构接口 |
| 独立复核模型 | 三份面板意见是**同族复核**（同一模型多角色），已在界面与审计里如实标注，不构成独立复核 |

---

## 8. 验收基线

一次完整验收应达到（均为最近一次实跑结果）：

| 检查 | 结果 |
|---|---|
| `scripts/smoke.py` 不变量 | 114 项全过 |
| `scripts/smoke_api.py` 接口层（进程内 ASGI） | 50 项全过 |
| `scripts/smoke_ops.py` 运营后台 | 77 项全过 |
| `scripts/smoke_auth.py` 鉴权（真实 HTTP） | 40 项全过 |
| `scripts/smoke_api_live.py` 真实 HTTP 端到端 | 57 项全过 |
| **合计** | **338 项** |
| `scripts/check_ui.cjs` 前端一致性 | 96 项 0 失败 |
| `scripts/check_retrieval.py` 检索验收 | 命中 15/15，**证据准入 15/15**，安全关键通过 |
| `scripts/check_schema.py` 建库与升级 | 36 项 0 失败 |
| `scripts/check_store_parity.py` 双存储一致 | 0 失败 |
| `scripts/check_env.py` 环境体检 | 18 通过 / 2 提醒 / 0 阻塞 |
| `scripts/multi_turn_cases.py` 跨轮行为 | 7 个案例全过 |

复跑方式见 [`Code/README.md`](Code/README.md) 的"怎么跑起来"。
